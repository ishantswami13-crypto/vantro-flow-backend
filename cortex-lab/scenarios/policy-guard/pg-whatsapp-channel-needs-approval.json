{
  "id": "policy-guard/pg-whatsapp-channel-needs-approval",
  "category": "policy-guard",
  "mode": ["static"],
  "description": "Any action on the 'whatsapp' channel must require approval (external message gate). Must be allowed but approval_required=true, safe_to_auto_execute=false.",
  "riskLevel": "medium",
  "expected": {
    "allowed": true,
    "blocked": false,
    "approval_required": true,
    "safe_to_auto_execute": false
  }
}
